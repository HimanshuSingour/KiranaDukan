
server:
  port: ${PORT:8888}
  address: 0.0.0.0

spring:
  profiles:
    active: dev

  datasource:
    url: jdbc:mysql://localhost:3306/KiranaDukan
    username: root
    password: Pavilion@12
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
      show-sql: true
    hibernate:
      ddl-auto: update

  main:
    web-application-type=none:
    allow-circular-references: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: algorithmxlr88@gmail.com
    password: kxivcojuormedwan
    properties:
      mail:
        smtp:
          starttls:
            enable: true
          auth: true

